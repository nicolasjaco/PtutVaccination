import { VaccinFilterPipe } from './vaccin-filter.pipe';

describe('VaccinFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VaccinFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
